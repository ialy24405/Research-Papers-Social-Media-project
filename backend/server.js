#!/usr/bin/env node

/**
 * ScholarStream Backend Server
 *
 * This script starts the ScholarStream backend server
 * Make sure to set up your environment variables before running
 */

require("ts-node/register");
require("./src/app.ts");
